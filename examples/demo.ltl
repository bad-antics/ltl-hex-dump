// examples/demo.ltl — runnable demo for ltl_hex_dump

import std::io
use ltl_hex_dump as lib

fn main() {
    io::println("ltl_hex_dump demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}